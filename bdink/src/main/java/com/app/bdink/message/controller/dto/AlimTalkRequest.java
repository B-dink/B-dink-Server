/*
package com.app.bdink.message.controller.dto;

public record AlimTalkRequest(
        String bizType,      // "at" (필수)
        String senderKey,    // 발신프로필 키 (필수)
        String templateCode, // 템플릿 코드 (필수)
        String to,           // 수신번호 (필수)
        String reqPhone,
        String reSubject,
        String reText,
        String text          // 메시지 내용 (필수)
) {}
*/